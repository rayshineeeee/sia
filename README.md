# sia

## plan
1. We need to make sure that the simple task actually works. To do this, first create the task data and the baseline.py, and make sure that the baseline.py actually works
2. Set up a basic Claude Code agent which is able to take this baseline.py and improve it. First, do it using the command line to make sure it works, and then do it programmatically using the SDK.