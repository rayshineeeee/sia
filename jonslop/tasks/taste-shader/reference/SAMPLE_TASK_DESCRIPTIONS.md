# Sample Task Descriptions

Generate a single-pass GLSL fragment shader whose rendered image matches Johnny's visual taste (CLIP-centroid), by writing a taste-conditioned instruction to a frozen LLM that returns the GLSL.
